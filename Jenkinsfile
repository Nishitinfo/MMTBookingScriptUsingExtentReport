pipeline {
    agent any

    stages {
        
      stage('Build and Test') {
    steps {
        // Use 'bat' for Windows command execution
        bat 'mvn clean test'
        }
     }
       
    }
}
